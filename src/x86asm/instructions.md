# Референс по инструкциям

Список инструкций x86 можно посмотреть на неофициальном[
сайте](https://www.felixcloutier.com/x86/) либо в официальном
[мануале](https://software.intel.com/en-us/download/intel-64-and-ia-32-architectures-sdm-combined-volumes-1-2a-2b-2c-2d-3a-3b-3c-3d-and-4)
--- это огромная pdf-ка на несколько тысяч страниц. Инструкций поистине много,
но нам понадобятся только некоторые. Сначала разберемся, как устроены их операнды.

# Типы операндов

Ассемблерные инструкции принимают _операнды_, которые могут быть нескольких
типов.
- Числовые константы. В документации они обычно обозначаются как `immXX`
  (immediate), где `XX` --- длина константы в битах.
- Регистры. Обозначаются как `rXX` (register), где `XX` --- битность регистра.
  Например, `r64` --- в некотором смысле аналог `uint64_t`.
- Адреса в памяти. Обозначение: `mXX` (memory), где `XX` кодирует `sizeof`
  типа, на который ссылается адрес. К примеру, `m64` --- нечто похожее на
  `uint64_t*`.

Например, посмотрим документацию по
[`add`](https://www.felixcloutier.com/x86/add). Одна из "перегрузок" этой
инструкции --- `add r/m8, r8` "add r8 to r/m8". То есть прибавляем 8-битный
регистр к другому регистру либо к 8-битному числу в памяти.

> Бывают еще другие типы операндов, например, абсолютные (`moffsXX`) или
> относительные (`relXX`) смещения. Они тоже некоторым образом задают адреса в
> памяти, но нам пока что не понадобятся.

Остановимся подробнее на регистрах и адресах.

## Регистры

Регистры --- это ячейки памяти внутри процессора, в которых он производит
вычисления. Конкретный набор регистров зависит от модели процессора. У
процесссоров x86 он примерно
[такой](https://en.wikipedia.org/wiki/X86#/media/File:Table_of_x86_Registers_svg.svg).

![x86_regs](x86_regs.svg)

Основное, что здесь можно увидеть:
1. General purpose регистры `rax`, `rbx`, `rcx`, `rdx`, `rdi`, `rsi` и
   `r8`--`r15`. Они используются как слоты для числовых переменных и указателей
   --- в основном нам потребуются именно они. Такие регистры 64-битные. В
   более старых процессорах были только 32-, 16- и 8-битные регистры, а в новых
   моделях они "расширялись". Например, `eax` --- младшие 32 бита `rax`, `ax`
   --- младшие 16 бит `eax`, а `al` (lower) и `ah` (higher) --- 8-битные
   половинки `ax`.
1. Системные регистры: `rflags` --- флаги, которые проставляются при сравнениях
   чисел и в результате арифметических операций, `rip` --- указатель на текущую
   инструкцию, `rsp` --- указатель на верхушку стека, `rbp` --- указатель на
   границу стекового фрейма. Их подробнее обсудим позже.
1. Широкие регистры `xmm0`--`xmm15`, `ymm0`--`ymm15` и `zmm0`--`zmm15`. Они
   тоже росли постепенно, поэтому вложены друг в друга. Такие регистры
   используются для "векторных" операций: например, в них можно записать
   несколько `int`-ов и потом попарно сложить как короткие векторы. Еще эти
   регистры по совместительству используются для вещественной арифметики, в них
   можно хранить `float` и `double`.
1. Стековые регистры сопроцессора [x87](https://en.wikipedia.org/wiki/X87)
   `st(0)`--`st(7)`. В более старых процессорах вещественная арифметика была
   реализована в отдельном
   [FPU](https://en.wikipedia.org/wiki/Floating-point_unit) --- сопроцессоре
   x87. Чтобы передать в него данные и получить обратно, использовались эти
   регистры.
1. Регистры с настройками --- `cr0`--`cr15`, `cs`, `ss`, `ds`, `es`, `fs` и
   многие другие. Например, `cr0` и `cr3`
   [влияют](https://wiki.osdev.org/Paging) на то, как процессор будет ходить в
   память, отображая
   [виртуальные](https://en.wikipedia.org/wiki/Virtual_memory) адреса
   (собственное адресное пространство для каждого процесса) в физические (общее
   адресное пространство внутри всей оперативной памяти).

## Адреса в памяти

Адреса задаются в ассемблере конструкцией вида `[base + index * scale +
offset]` (все, кроме `base` --- опциональное). Чтобы ее понять, можно провести
аналогию: есть массив структур аллоцированный по адресу `base`. Размер одной
структуры --- это `scale`, `index` --- индекс элемента массива, а `offset` ---
смещение, чтобы выбрать какое-то поле внутри структуры. Поэтому есть
ограничения: `scale` и `offset` должны быть константами (`sizeof` структуры и
смещения полей известны в compile time). Также считается, что `scale` может
быть только 1, 2, 4 или 8 🤷. А `base` и `index` могут быть либо константами,
либо регистрами.

# Инструкции

Можно неформально разделить инструкции на 4 группы:
1. доступ к памяти,
1. арифметика и логика,
1. прыжки между адресами и метками в коде.
1. стек и вызовы функций.

## Доступ в память

Инструкция [`mov`](https://www.felixcloutier.com/x86/mov) позволяет
перекладывать значения между регистрами и оперативной памятью. Сколько байт
нужно прочитать или записать выводится из типа адреса и битности регистра.

| Пример | Смысл |
| --- | --- |
| `mov eax, 42` | Записать 42 в регистр `eax`. Старшие биты `rax` при этом обнуляются. |
| `mov rbx, rax` | Записать значение регистра `rax` в `rbx`. |
| `mov [rax + rbx], ecx` | Записать значение `ecx` (`uint32_t`) по адресу `rax + rbx`. |
| `mov cx, [rdi + 61]` | Прочитать значение (`uint16_t`) из адреса `rdi + 61` и положить в регистр `cx`. |
| `mov byte ptr [rax], 52 ` | Записать 52 (`uint8_t`) по адресу `rax`. |
| `mov word ptr [rax], 52 ` | Записать 52 (`uint16_t`) по адресу `rax`. |
| `mov dword ptr [rax], 52 ` | Записать 52 (`uint32_t`) по адресу `rax`. |
| `mov qword ptr [rax], 52 ` | Записать 52 (`uint64_t`) по адресу `rax`. |


При записи констант в память нужно явно указывать тип адреса
(`byte/word/dword/qword ptr`), поскольку результат операции будет разным.
Например, `mov byte ptr [rax], 52` запишет единственный байт `0x34`, а `mov
dword ptr [rax], 52` --- байт `0x34` и после еще три нулевых байта
(представление `uint32_t` в little endian).

> Формально все виды `mov` разные (например, у них будут опкоды разной длины),
> но обозначаются они общим мнемоником `mov`.

По техническим причинам нельзя переложить значение между двумя адресами за одно
действие. Вместо этого нужно явно написать две инструкции.

```x86asm
mov rax, [rbx]
mov [rcx], rax
```

### Вычисление адресов

Инструкция [`lea`](https://www.felixcloutier.com/x86/lea) (load effective
address) позволяет положить в регистр числовое значение адреса, записанного в
форме `[base + index * scale + offset]`. Некоторые компиляторы абьюзят эту
возможность и выражают сложение через `lea` вместо `add`.

```x86asm
aplusb:
    lea rax, [rdi + rsi]
```

## Арифметика

На x86 числа в памяти и в регистрах представляются в [little
endian](../coding/ints.md#endianness). Для кодирования знаковых интов также
используется [two's complement](../coding/ints.html#Знаковые-инты).

Иногда число хранится по частям в паре регистров --- да-да, не удивляйтесь 🤯,
--- это обозначается как `hi:lo` (старшие биты --- в `hi`, младшие --- в `lo`).

Обозначим за `DEST` операнд, в который записывается результат операции, а
вспомогательный операнд за `SRC`.

| Инструкция | Пример | Смысл |
| --- | --- | --- |
| [`inc DEST`](https://www.felixcloutier.com/x86/inc) | `inc rax` | `DEST += 1`. Инкремент регистра `rax`. |
| [`dec DEST`](https://www.felixcloutier.com/x86/dec) | `dec dword ptr [rbx]` | `DEST -= 1`. Декремент `uint32_t` по адресу `rax`. |
| [`add DEST, SRC`](https://www.felixcloutier.com/x86/add) | `add rax, rbx` | `DEST += SRC`. Прибавляем `rbx` к `rax`. |
| [`sub DEST, SRC`](https://www.felixcloutier.com/x86/sub) | `sub eax, [rcx]` | `DEST -= SRC`. Вычитаем из `eax` `uint32_t` по адресу `rcx`. |
| [`neg DEST`](https://www.felixcloutier.com/x86/neg) | `neg eax` | `DEST = -DEST`. Меняем знак `eax`, интерпретируя его как `int32_t`. |
| [`mul SRC`](https://www.felixcloutier.com/x86/mul) | `mul rcx` | `rdx:rax = rax * SRC`. Беззнаковое умножение. В данном случае умножаем `rax` на `rcx`, интерпретируя их как `uint64_t`. Результат записываем в пару регистров `rdx:rax`. |
| [`imul SRC`](https://www.felixcloutier.com/x86/imul) | `imul rcx` | Знаковое умножение. То же самое, но интерпретируем `rcx` и `rax` как `int64_t`. |
| [`div SRC`](https://www.felixcloutier.com/x86/div) | `imul rdi` | Беззнаковое деление `rdx:rax` на `SRC`. Частное сохраняется в `rax`, остаток --- в `rdx`. В нашем случае интерпретируем `rdx:rax` как `uint128_t` и делим на `rcx` как на `uint64_t`. |
| [`idiv SRC`](https://www.felixcloutier.com/x86/div) | `imul rdi` | Знаковое деление. То же самое, но интерпретируем `rdx:rax` как `int128_t` и делим на `rcx` как на `int64_t`. |

Сложение и вычитание работают одинаково для знаковых и беззнаковых чисел,
поскольку используется two's complement. А вот деления и умножения уже нужны
отдельные знаковые и беззнаковые версии.

### Знаковое расширение

Иногда бывает нужно переложить число из маленького регистра в более широкий
(например, `eax` → `rcx`). Для беззнаковых чисел достаточно сделать `mov rcx,
rax`, т.к. старшие биты `rax` будут нулевыми, и это как раз то, что нужно.

Если число знаковое, так уже не получится. Например, битовое представление
`uint32_t(-1) = 2^32 - 1`, а `uint64_t(-1) = 2^64 - 1` --- совсем другое. Чтобы
корректно переложить знаковое число в более широкий регистр, есть инструкция
[`movsxd`](https://www.felixcloutier.com/x86/movsx:movsxd). Она реализует такой
"каст" знаковых интов.

## Логика и побитовые операции

Тут все просто.

| Инструкция | Пример | Смысл |
| --- | --- | --- |
| [`and DEST, SRC`](https://www.felixcloutier.com/x86/and) | `and rax, rbx` | `DEST &= SRC`. Побитовое "и". |
| [`or DEST, SRC`](https://www.felixcloutier.com/x86/or) | `or rax, rbx` | `DEST \|= SRC`. Побитовое "или". |
| [`not DEST, SRC`](https://www.felixcloutier.com/x86/or) | `not rax, rbx` | `DEST = ~DEST`. Побитовое "не". |
| [`shl DEST, SRC`](https://www.felixcloutier.com/x86/sal:sar:shl:shr) | `shl rax, 1` | `DEST <<= SRC`. Битовый сдвиг влево. |
| [`shr DEST, SRC`](https://www.felixcloutier.com/x86/sal:sar:shl:shr) | `shr rax, 1` | `DEST >>= SRC`. Битовый сдвиг вправо. |

## Прыжки

Инструкция [`jmp`](https://www.felixcloutier.com/x86/jmp) позволяет прыгнуть на
метку или адрес функции и продолжить исполнение оттуда. Сама по себе она не
очень полезна, т.к. будет исполняться всегда. Бывают еще "условные"
варианты `jmp` --- [`jCC`](https://www.felixcloutier.com/x86/jcc), где `CC` ---
условие. Они позволяют в ассемблере моделировать `if` и `for`.

Инструкции `jCC` читают битовые флаги процессора в регистре `rflags` и в
зависимости от них могут выполнить прыжок, либо ничего не сделать.
[Флаги](https://en.wikipedia.org/wiki/FLAGS_register) проставляются
автоматически большинством инструкций. Например, арифметические инструкции
проставляют флаги: [`OF`](https://en.wikipedia.org/wiki/Overflow_flag)
(произошло переполнение), [`ZF`](https://en.wikipedia.org/wiki/Zero_flag)
(получился ноль), [`SF`](https://en.wikipedia.org/wiki/Negative_flag)
(получилось отрицательное число),
[`PF`](https://en.wikipedia.org/wiki/Parity_flag) (получилось четное число) и
многие другие.

Также флаги проставляет инструкция [`cmp DEST,
SRC`](https://www.felixcloutier.com/x86/cmp). Флаги будут такими же, как если бы
выполнилось `sub DEST, SRC`, только значение `DEST` не перезапсывается.
После сравнения можно сделать условный прыжок и узнать, как соотносились операнды.

`LABEL` --- метка, на которую будем прыгать.

| Инструкция | Условие для прыжка |
| --- | --- |
| `je LABEL` | `DEST == SRC`, "equal" |
| `jne LABEL` | `DEST != SRC`, "not equal" |
| `jl LABEL` | `DEST  < SRC`, знаковый "less" |
| `jle LABEL` | `DEST <= SRC`, знаковый "less or equal" |
| `jg LABEL` | `DEST > SRC`, знаковый "greater" |
| `jge LABEL` | `DEST >= SR`, знаковый "greater or equal" |
| `jb LABEL` | `DEST  < SRC`, беззнаковый "below" |
| `jbe LABEL` | `DEST <= SRC`, беззнаковый "below or equal" |
| `ja LABEL` | `DEST > SRC`, беззнаковый "above" |
| `jae LABEL` | `DEST >= SR`, беззнаковый "above or equal" |

Отдельно существуют знаковые и беззнаковые сравнения, т.к. знаковые инты
кодируются в two's complement. На уровне ассемблера непонятно, знаковые или
беззнаковые числа записаны в память. Поэтому явно нужно указывать в условии,
как интерпретировать их биты.

### Небольшие примеры

| Си | Ассемблер x86 |
| --- | --- |
| <pre><code class="language-c hljs">if (rax <= 13) {<br>  rbx = 52;<br>}<br></code></pre> | <pre><code class="language-x86asm hljs">  cmp rax, 13<br>  jg .Lfalse_branch<br><br>.Ltrue_branch:<br>  mov rbx, 52<br><br>.Lfalse_branch:<br>  ...</code></pre> |
| <pre><code class="language-c hljs">int rax = 0;<br>for (int rcx = 0; rcx < 128; ++rcx) {<br>  rax += rcx;<br>}</code></pre> | <pre><code class="language-x86asm" hljs>  mov eax, 0<br>  mov ecx, 0<br><br>.Lloop_body:<br>  add eax, ecx<br>  inc ecx<br><br>  cmp ecx, 128<br>  jl .Lloop_body</code></pre> |
| <pre><code class="language-c hljs">while (true) {<br>}</code></pre> | <pre><code class="language-x86asm hljs">.Linfinite_loop<br>  jmp .Linfinite_loop</code></pre> |

